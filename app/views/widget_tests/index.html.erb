<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />
<body>
</body>

<%
  user_id = 1
  user_hash = OpenSSL::HMAC.hexdigest(
    'sha256',
    @web_widget.hmac_token,
    user_id.to_s
  )

%>
<script>

window.fillnodeSettings = {
  hideMessageBubble: false,
  // showUnreadMessagesDialog: false,
  // baseDomain: '.loca.lt',
  position: '<%= @widget_position %>',
  locale: 'en',
  useBrowserLanguage: true,
  type: '<%= @widget_type %>',
  // showPopoutButton: true,
  widgetStyle: '<%= @widget_style %>',
  darkMode: '<%= @dark_mode %>',
};

(function(d,t) {
  var BASE_URL = '';
  var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
  g.src= BASE_URL + "/packs/js/sdk.js";
  g.async = true;
  s.parentNode.insertBefore(g,s);
  g.onload=function(){
    window.fillnodeSDK.run({
      websiteToken: '<%= @web_widget.website_token %>',
      baseUrl: BASE_URL
    })
  }
})(document,"script");

window.addEventListener('fillnode:ready', function() {
  console.log('fillnode:ready', window.$fillnode);
  if (window.location.search.includes('setUser')) {
    window.$fillnode.setUser('<%= user_id %>', {
      identifier_hash: '<%= user_hash %>',
      email: 'jane@example.com',
      name: 'Jane Doe',
      phone_number: ''
    });
  }
})

window.addEventListener('fillnode:error', function(e) {
  console.log('fillnode:error', e.detail)
})


window.addEventListener('fillnode:on-message', function(e) {
  console.log('fillnode:on-message', e.detail)
})

window.addEventListener('fillnode:postback', function(e) {
  console.log('fillnode:postback', e.detail)
})

window.addEventListener('fillnode:opened', function() {
  console.log('fillnode:opened')
})

window.addEventListener('fillnode:closed', function() {
  console.log('fillnode:closed')
})

window.addEventListener('fillnode:on-start-conversation', function(e) {
  console.log('fillnode:on-start-conversation', e.detail)
})
</script>
